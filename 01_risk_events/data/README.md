# 01 数据目录

本目录保存本机计算所需的数据，不随源码提交。

- `02输入/`：供离线02使用的风险事件、计算清单、交易日历、ST状态、证券范围及生命周期。属于正式计算输入，不按运行日志清理。
- `outputs/`：01计算生成的结果及过程文件；每次运行创建新批次，结果位置以当次输出为准。

联网快捷入口会准备本次风险数据；离线评价必须保留有效交接清单及其引用文件。已删除的批次不能直接续跑。
