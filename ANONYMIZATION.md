# 匿名化说明

本包是独立源码副本，不包含本地业务输入、原始打分Excel、行情、市场底座、运行结果、凭据、虚拟环境或Git历史。原始工作目录不受本次导出影响。

## 标识与行为

- 券商名称统一为 `券商01` 至 `券商21`。单模型采用 `券商03`；编号不随包附带真实机构对应表。
- 单模型的函数、常量、命令参数及数据目录采用 `single` / `SINGLE` 等通用标识，文件名与引用一起修改。
- 证券强制覆盖默认关闭，测试仅用虚构证券验证该机制。真实使用时需自行提供业务覆盖规则。
- 保留日期衔接、前一交易日取值、风险阈值、档位映射和指标公式。名称匿名化不等于这些业务方法也已隐藏。
- RQData、Choice及相关SDK名称保留，它们属于第三方接口标识；本包没有授权信息。
- 原始业务文件不能不改列名就直接用于此副本。请使用自己的匿名格式输入，本地原版本仍按原列名运行。

## 离线验证

在具备项目依赖的Python中，从本目录运行：

```bash
python -B 01_risk_events/acceptance.py --source-only
python -B 02_peer_benchmark/acceptance.py --source-only
python -B -m unittest discover -s 快捷运行/tests -q
python -B -m unittest discover -s 02_peer_benchmark/data/月截面数据转化为逐日数据 -p test_conversion.py -q
```

验证仅覆盖源码完整性、模块导入和虚构样例。双击界面、目标电脑兼容性及真实线上取数需另行验证，不能将源码验证等同于业务验收。

## 上传范围

上传本目录的内容即可，保留各级 `.gitignore`。不要加入数据、账号、原始名称对应表或导出前的本地文档。根目录README是业务说明，`快捷运行/先读我.md`是匿名入口说明。压缩包不包含仓库历史。
